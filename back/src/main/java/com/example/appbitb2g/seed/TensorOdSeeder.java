/*
*
*
* Tensor Od ----- es lo mismo que FlujoOD?


package com.example.appbitb2g.seed;

import com.example.appbitb2g.repository.TensorOdSeederRepository;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.context.annotation.Profile;
import org.springframework.stereotype.Component;

import java.util.List;

@Component
@Profile("dev")
public class TensorOdSeeder {

    @Autowired private TensorOdSeederRepository tensorOdSeederRepository;

    public void seed() {
        if (tensorOdSeederRepository.count() > 0)
            return;

        tensorOdSeederRepository.saveAll(List.of(


                )}



}

 */
