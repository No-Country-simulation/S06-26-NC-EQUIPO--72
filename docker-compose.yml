services:
  backend:
    build:
      context: ./back
      dockerfile: Dockerfile
    container_name: app-bit-b2g
    restart: always
    ports:
      - "8080:8080"
    networks:
      - bit-b2g-network
  

  react-frontend:
    container_name: react_app
    build:
      context: ./front
      dockerfile: Dockerfile
    ports:
      - "5173:5173"
    networks:
      - bit-b2g-network


     
networks:
  bit-b2g-network:  

     